substitutions:
  variant: esp32c3
  # Metadata/backward-compatible value only; packages/esp32.yaml no longer
  # passes `board` to the ESPHome esp32 component.
  board: esp32-c3-devkitm-1

packages:
  esp32: !include esp32.yaml
